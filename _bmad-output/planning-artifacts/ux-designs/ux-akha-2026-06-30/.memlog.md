---
topic: Akha UX — calm task companion (focus view + creature + restraint)
updated: 2026-06-30T17:28
---

- (decision) Working mode: Hybrid — fast-path EXPERIENCE.md (derivable from strong PRD), coaching-path DESIGN.md (creature + palette benefit from seeing options).
- (decision) Palette scope: design the FULL palette system for v1 per NFR8 — multiple calm schemes, each with a matching coloured creature, plus a white option and colourblind-safe palettes. Resolves tension with shipped single dark deep-sea-blue theme (that becomes the default scheme within the system).
- (assumption) Form factor = MOBILE (iOS + Android), a WebView-wrapped app. Evidence: src/index.css safe-area insets (env(safe-area-inset-*)), .pb-nav bottom-nav clearance, overscroll-behavior:none, -webkit-overflow-scrolling:touch; addendum cites identical iOS/Android pricing. Treated as established unless Ryan corrects.
- (event) Existing shipped visual identity captured from tailwind.config.js + src/index.css: dark-only deep-sea-blue structure scale (page bg #04141f, raised surface ~#0a3450), SINGLE teal accent (primary #008080, highlight #69c4d2, ~10% of screen), success green semantic-only (#2D6D54), tactile depth (.surface-raised top-lit gradient cards, .btn-accent-3d 3D-lip button, .input-glow, recessed .checkbox), type = Playfair Display (headings/display, serif) + Inter (body), --radius 0.75rem. Low-sat category tints exist (work/personal/health/shopping/other).
- (event) Creature = a MONKEY. Art is a painterly multi-path VTracer SVG (~243 paths) under public/companion/ with poses: sit_smile, sit_laugh, grin, clasp_laugh, stand. Rendered in deep-sea-blue tones (#03243A body / #FDFDFD face). Companion.jsx: slow 5.5s breath idle, gentle hop (scale 1.04, y-2) + soft teal glow on reaction, static blurred ground shadow, ~1.6s self-dismissing line. Resting pose currently monkey_sit_laugh.svg.
- (assumption) CONSTRAINT for architecture: NFR8 'matching coloured creature per palette' is non-trivial — the monkey art is baked in deep-blue fills across 243 paths. Multi-palette matching creatures will need a recolor strategy (CSS hue/filter retint, per-palette art exports, or a recolorable single-fill silhouette). Flag, do not solve in UX.
- (change) Reconcile: PRD FR16/FR25 say 'progress WHEEL'; shipped Companion.jsx renders a horizontal BAR (RING_TARGET=8). DESIGN/EXPERIENCE should specify the wheel (accumulation-only, no denominator) as the intended form; bar is the shipped placeholder.
- (event) Existing IA (src/App.jsx + BottomNav.jsx): bottom nav = Today(/) | Upcoming | center +FAB (focuses #task-quick-input on Today; FAB never directly creates, it's the single create path) | Browse | Settings. A Completed (archive) page exists but is NOT in the bottom nav. Auth pages: Login/Register/Forgot/Reset. AppLayout = header + bottom nav. /preview renders CompanionPreview (dev win-moment harness).
- (assumption) IA reconciliation needed: PRD reorganizes around Focus view (one task = the doing) / Capture-planning (neutral, full list OK) / Archive / Settings / Onboarding. Existing surfaces Today/Upcoming/Browse/Completed must remap onto that model. Pending Ryan's call on the nav structure.
- (decision) Nav model = 4 surfaces: Focus (home/, the one-task doing view) | Plan (full backlog + capture, neutral utility; absorbs the old Upcoming+Browse) | Archive (Completed; calm 'what I did') | Settings. Center +FAB stays as the single create path (focuses quick-input). Focus view replaces the old Today list; the full backlog never appears on the Focus surface (restraint contract §5).
- (decision) Default colour scheme = Deep Sea (the shipped dark deep-sea-blue + teal). Confirmed by Ryan.
- (decision) Onboarding palette set (FR1) = 7 schemes: Deep Sea (default), Forest/Sage, Twilight, Soft Paper (light/white, NFR8), Accessible (colourblind-safe, NFR8), + NEW Soft Pink (muted grown-up dusty rose, NOT cutesy per brand rule 6), + NEW Sky Blue (airy daytime, distinct from Deep Sea's nocturnal depth). Warm Lamplight DROPPED.
- (assumption) SCHEMA GAPS for architecture (Base44 Task/User entities): (1) Task has NO priority field — FR5 needs priority 1-5. (2) Task has NO explicit order/sort field — FR12 manual-rearrange override needs one. (3) User entity stores only 'role' — onboarding prefs (name-to-be-called, colour scheme, creature) have no home yet (FR1/FR3). Task.subtasks supports a 2nd nested level but PRD = one level deep (nested unused in v1). Flag, do not solve in UX.
- (assumption) TENSION: Task.category enum (Work/Personal/Shopping/Health/Other) exists in shipped data + has low-sat tints in tailwind, but the PRD's model is PRIORITY-driven and never mentions categories. The ordering rule (priority->due->added) does not use category. Decision pending: keep categories as quiet optional tags, or drop from v1. Pending Ryan.
- (decision) Palette set CONFIRMED by Ryan (7 schemes). Final hex anchors — Deep Sea(default,dark): bg#04141f surface#0a3450 accent#008080 hover#00a1a1 highlight#69c4d2 success#2D6D54 creature#03243A. Forest/Sage(dark): accent#8aa98a creature#243029. Twilight(dark): accent#a99cd6 creature#2a2440. Soft Paper(light): accent#3f8f8a creature#cbb89a. Accessible(colourblind-safe,dark): accent#e89c4a success#4a8fb0 creature#2c3e52. Soft Pink(light): accent#a86b72 success#4d7a63 creature#c7a3a0. Sky Blue(light): accent#3f86c4 creature#aac4dc. Spread = 4 dark / 3 light. Artifact: .working/color-themes-2.html.
- (assumption) [DEFAULT, Ryan deferred] No-subtask task in Focus: shows as one completable line; checking it auto-completes the task and fires the TASK tier (little dance + progress wheel + dim & sink). Whole task done = whole-task win.
- (assumption) [DEFAULT, Ryan deferred] Priority (1-5) is HIDDEN in the Focus view — set during planning, drives ordering silently; the doing surface never shows the number (avoids importing ranking-pressure).
- (assumption) [DEFAULT, Ryan deferred] Categories: keep the Task.category field in the schema (no migration) but show NO category UI in v1 — pure priority model on-screen per the PRD. Tints remain available if revived later.
- (decision) Visual language: EXTEND the shipped identity, do not reinvent. Keep tactile depth (.surface-raised top-lit gradient cards, .btn-accent-3d 3D-lip primary button, .input-glow focus, recessed .checkbox with success-green done state), lucide-react line icons, --radius 0.75rem, Playfair Display (serif headings/display) + Inter (body). Each of the 7 schemes re-skins these same component primitives.
- (decision) Progress wheel (FR16/FR25): a CIRCULAR accumulation ring filled in the accent, replacing the shipped horizontal bar. It only ever fills/accumulates as completions rise — NO denominator, percentage, number, 'remaining', shortfall cue, or cross-day comparison. Lives on the Focus surface near the creature.
- (decision) Settings surface (NFR9, depth lives here not onboarding): change name-to-be-called, colour scheme (7), creature (presets); reduced-motion follows the OS (NFR3); notifications toggle that ALWAYS starts OFF (NFR7); account + data/privacy (single-user private, NFR10). Onboarding stays minimal = 3 choices + welcome + first-open beat.
- (decision) Voice & tone baseline: warm, brief, quiet, plainspoken; never cutesy, never baby-talk, no hype/exclamation pile-ups (brand rule 6). Existing companion lines kept as the register: 'Nice.' / 'That's one done.' / 'Good move.' / 'Onward.' / 'Mm — well done.' / 'There you go.' Idle: 'Here with you today.' Carry-forward phrasing = a quiet 'still needs doing', never 'overdue'.
- (decision) Ryan CONFIRMED the three focus-view defaults: (1) no-subtask task fires the task-tier little dance, (2) priority hidden on Focus, (3) categories kept-in-schema/no-UI v1. Left tagged as assumptions in-spine so they can be revisited at build if needed.
- (decision) Reviewer gate: run all 3 lenses (restraint/brand-fidelity, accessibility, rubric walker), then finalize. (User also ticked 'skip'; resolved as run-then-finalize.)
- (decision) Key-screen mocks: render Focus + onboarding + Plan in Deep Sea (default), promote to mockups/.
- (event) Reviewer gate ran 3 lenses. Restraint: PASS w/ conditions (0 blockers, 3 should-fix). Rubric: clean PASS (0 dangling refs, 0 uncovered FR/NFR, 0 contradictions). Accessibility: conditional pass (2 blockers, 5 should-fix). Findings in review-restraint.md / review-rubric.md / review-accessibility.md. All findings are strengthening corrections, none alter Ryan's confirmed picks. Resolving into the spines.
- (decision) Resolution: applying all restraint should-fixes + accessibility blockers/should-fixes to both spines (wheel no-closure/not-a-goal; carry-forward named token + aggregate non-escalation; accent=fill/large-only with highlight/accent-text for links; ≥3:1 affordance borders; structural active-subtask signal; full reduced-motion table; SR + focus-order patterns; Archive visual guardrail in DESIGN; fences for decompose-nudge, companion-line selection, destructive token, notifications copy). Confirmed palette hexes preserved; only functional affordance borders adjusted for ≥3:1.
- (event) spines finalized
- (event) ux run closed
